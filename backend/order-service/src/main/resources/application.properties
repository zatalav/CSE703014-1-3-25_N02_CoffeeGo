server.port=8086
spring.application.name=order-service

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.maximum-pool-size=10

spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5181,http://localhost:5180,http://localhost:5173,http://localhost:3000,http://localhost:4000,http://localhost:8080}
app.jwt.secret=${JWT_SECRET:coffee-chain-management-secret-key-change-me-in-production-2026}
app.jwt.access-token-minutes=${JWT_ACCESS_TOKEN_MINUTES:120}
app.jwt.refresh-token-days=${JWT_REFRESH_TOKEN_DAYS:7}

springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

# Sandbox payment gateway. Replace these with merchant credentials issued for
# your own VNPay sandbox account before sharing a test build.
payment.vnpay.pay-url=${VNPAY_PAY_URL:https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}
payment.vnpay.tmn-code=${VNPAY_TMN_CODE:V43YHED6}
payment.vnpay.hash-secret=${VNPAY_HASH_SECRET:LG1VHYRGRKD0G9IFJEKFT5D3C938OAKO}
payment.vnpay.return-url=${VNPAY_RETURN_URL:https://2jj6j9p9-5181.asse.devtunnels.ms/payment-return}
